package moonbox.catalyst.adapter.mongo.schema

import moonbox.catalyst.core.{Schema, SchemaFactory}

class MongoSchemaFactory extends SchemaFactory{
  override def create(props: Map[String, String]): Schema = {
    // TODO:
    null
  }
}
